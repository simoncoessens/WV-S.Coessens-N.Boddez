mf = 6;
i = 3;

err = zeros(4,1);
count = 1;
for n = 1000:100000:10000
    for j = 1:i
        err(count) = err(count) + group_tester(n,0.05,mf);
        err(
    end
    count = count + 1;
end

err = err/i;
plot(err)